#include <iostream>
#include <vector>

using namespace std;

vector<int> scal(vector<int> left, vector<int> right){

	int size_left = left.size();
	int right_left = right.size();
	
	vector<int> result;
	// result.push_back(value) dodaje nowy element na koniec tablicy
	// vector<int> result(size_left+size_right;)
	
	// Scalanie
	
	return result;
}

vector<int> sort_scal(vector<int> tab){

	// warunek stopu

	vector<int> left, right;
	
	left = sort_scal(lewy,...);
	right = sort_scal(right, ...);
	
	vector<int> result = scal(left,right);
	
}

int main(){
	return 0;
}
